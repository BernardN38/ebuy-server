version: "2"
sql:
  - schema: schema.sql
    queries: query.sql
    engine: postgresql
    gen:
      go:
        package: orders_sql
        out: orders
        json_tags_case_style: "camel"
        emit_json_tags: true
