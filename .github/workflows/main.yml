name: Docker Compose Actions Workflow
on: push
jobs:
  test:
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v2
      - name: cd into directory
        run: cd project
      - name: Build the stack
        run: make up_build_all
